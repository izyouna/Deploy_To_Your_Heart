# Deploy to Your Heart

## Agent skills

### Issue tracker

Issues and PRDs live as local markdown files in `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage state is recorded as status strings (default vocabulary). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo structure with one `CONTEXT.md` and `docs/adr/` at root. See `docs/agents/domain.md`.
